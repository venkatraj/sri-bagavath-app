import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const EBooksScreen = (props) => {
  return (
    <View>
      <Text>EBooksScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default EBooksScreen;
