import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const MagazinesScreen = (props) => {
  return (
    <View>
      <Text>Magazines Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default MagazinesScreen;
