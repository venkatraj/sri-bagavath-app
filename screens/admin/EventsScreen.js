import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const EventsAdminScreen = (props) => {
  return (
    <View>
      <Text>EventsAdminScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default EventsAdminScreen;
